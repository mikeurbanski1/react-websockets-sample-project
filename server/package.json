{
  "name": "anonymus-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf build && mkdir build && tsoa routes && tsc --outDir build --experimentalDecorators",
    "start": "node build/src/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "tsoa": "^5.1.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.9.5",
    "typescript": "^5.3.2"
  }
}